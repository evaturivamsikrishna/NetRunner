name: 🕹 NetRunner Website Monitor & Dashboard (Netlify)

on:
  schedule:
    - cron: "0 */3 * * *"   # ⏰ Every 3 hours
  workflow_dispatch:         # 🧠 Allow manual run
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: 🧩 Full Monitor + Dashboard Build
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || true
          pip install pandas requests beautifulsoup4

      - name: 🧰 Prepare Directories
        run: |
          mkdir -p reports generated logs

      - name: 🌍 Run NetRunner Website Monitor
        run: |
          echo "🚀 Running website health checks..."
          python -m src.main || true

      - name: 📊 Build Analytics Metrics
        run: |
          echo "📈 Generating metrics and charts..."
          python -m src.analytics.metrics_builder || true
          python -m src.analytics.chart_generator || true
          echo "✅ Dashboard analytics complete."

      - name: 💾 Commit Updated Reports & Dashboard
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports generated || true
          git commit -m "🔁 Auto-update metrics & dashboard [skip ci]" || echo "No changes to commit."
          git push origin main || echo "⚠️ No push access — skipping."

      - name: 🌐 Trigger Netlify Deployment
        if: success()
        run: |
          echo "🔁 Triggering Netlify site rebuild..."
          if curl -X POST -d '{}' ${{ secrets.NETRUNNER_NETLIFY_HOOK }}; then
            echo "✅ Netlify deployment triggered successfully."
          else
            echo "⚠️ Netlify trigger failed. Please check hook URL or secrets."
          fi